// const urlSearch = window.location.search;
// const currentParamsElem = document.getElementById("current-params");
// currentParamsElem.textContent = urlSearch;

// //базовий запобіжник щоб не зберігався пустий рядок
// if (urlSearch) {
//   currentParamsElem.textContent = urlSearch;
//   localStorage.setItem("myParams", urlSearch);
// } else {
//   currentParamsElem.textContent =
//     localStorage.getItem("myParams") || "Параметри відсутні";
// }
